{
	"dependencies": {
		"react": "^18.2.0",
		"react-dom": "^18.2.0"
	},
	"devDependencies": {
		"esbuild": "^0.20.2"
	},
	"name": "@sampleworkspace/invitation-form",
	"private": true,
	"scripts": {
		"build": "node -e \"require('fs').rmSync('build/static',{recursive:true,force:true})\" && esbuild src/index.js --outdir=build/static --bundle --entry-names=[name].[hash] --format=esm --loader:.js=jsx"
	},
	"version": "0.0.0"
}
